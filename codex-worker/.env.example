# OpenAI (Codex) API key, used by codex-cli inside the codex-worker container.
# Create one at https://platform.openai.com and paste it here.
# Keep this file chmod 600; never commit the real value.
CODEX_API_KEY=<YOUR_OPENAI_API_KEY>
