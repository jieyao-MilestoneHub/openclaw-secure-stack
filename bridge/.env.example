# Bearer token used by gateway → bridge over the internal docker network.
# Generate a fresh value with:
#   openssl rand -hex 32
# Store the generated token here; file must be chmod 600 and owned by the
# user running docker compose.
BRIDGE_TOKEN=<64_HEX_CHARS_GENERATED_LOCALLY>
